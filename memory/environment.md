# Environment Notes

## Rust
- **Status**: INSTALLED (v1.94.0 via Homebrew on 2026-03-11).
- **Required**: `rustc`, `cargo` (available in path).
- **Root workspace**: `Cargo.toml` exists at project root, defines `engine` member.

## Node.js / npm
- **Status**: Available at `/opt/homebrew/bin/npm`.
- **Frontend**: `explorer/` initialized with Vite + React + TypeScript.
- **Dependencies**: Installed (177 packages).

## Homebrew
- **Status**: Available at `/opt/homebrew/bin/brew`.
